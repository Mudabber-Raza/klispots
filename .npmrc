# Force npm to use legacy peer deps resolution
legacy-peer-deps=true

# Disable optional dependencies that cause issues
optional=false

# Use npm's new resolution algorithm
resolution-mode=highest

# Disable platform-specific binaries that cause issues
target_platform=linux
target_arch=x64
target_libc=glibc
