# Handle optional dependencies properly
optional=false
# Use npm ci for production builds
engine-strict=true
# Force clean installs
prefer-offline=false
# Exclude problematic rollup dependencies
omit=optional
